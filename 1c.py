#Input : The program accepts two positive integers, b and h, one on each line, corresponding to the
#base and the height of the triangle, respectively.

#Output : On a single line, print out the area of the triangle

b = int(input('Enter base:'))
h = int(input('Enter height:'))
print(str((b * h)/2))